{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} {{ title }}{% endblock %}

{% block body %}
<div class='container'>
    <h1 class='my-2'>{{ title }}</h1>
    <div class='py-5'>
        {% if text.isPublished and text is defined %}
            {{ text.content|raw }}
        {% else %}
            <h2>Dans très peu de temps ici il y aura un text</h2>
        {% endif %}
    </div>
</div>
{% endblock %}
